---
import BrowseByCategoryCard from "../cards/BrowseByCategoryCard.astro"

type CategoryCard = {
    src: string,
    title: string,
    color: string,
    href: string,
}

interface Props {
    cards: CategoryCard[]
}

const { cards } = Astro.props
---
