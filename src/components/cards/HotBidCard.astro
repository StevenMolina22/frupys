---
import ETH from "../icons/ETH.astro";
import Heart from "../icons/Heart.astro";

interface Props {
    title: string;
    image: string;
    price: number;
    currentBid: number;
    likes: number;
}

const { title, image, price, currentBid, likes } = Astro.props;
---

<article
    class="block rounded-2.5xl border border-jacarta-100 bg-white p-[1.1875rem] transition-shadow hover:shadow-lg dark:border-jacarta-700 dark:bg-jacarta-700"
>
    <figure>
        <a href="item">
            <img
                src={image}
                alt={title}
                width="230"
                height="230"
                class="w-full rounded-[0.625rem] size-[230px] object-cover"
                loading="lazy"
            />
        </a>
    </figure>
    <div class="mt-4 flex items-center justify-between">
        <a href="item">
            <span
                class="font-display text-base text-jacarta-700 hover:text-accent dark:text-white"
                >{title}</span
            >
        </a>
        <span
            class="flex items-center whitespace-nowrap rounded-md border border-jacarta-100 py-1 px-2 dark:border-jacarta-600"
        >
            <span data-tippy-content="ETH">
                <ETH />
            </span>
            <span class="text-sm font-medium tracking-tight text-green"
                >{price}</span
            >
        </span>
    </div>
    <div class="mt-2 text-sm">
        <span class="dark:text-jacarta-300">Precio: </span>
        <span class="text-jacarta-700 dark:text-jacarta-100"
            >$ {currentBid}</span
        >
    </div>
    <div class="mt-8 flex items-center justify-between">
        <button
            type="button"
            class="font-display text-sm font-semibold text-accent"
            data-bs-toggle="modal"
            data-bs-target="#placeBidModal"
        >
            Lo quiero
        </button>
        <div class="flex items-center space-x-1">
            <span
                class="js-likes relative cursor-pointer before:absolute before:h-4 before:w-4 before:bg-[url('./img/heart-fill.svg')] before:bg-cover before:bg-center before:bg-no-repeat before:opacity-0"
                data-tippy-content="Favorito1"
            >
                <Heart />
            </span>
            <span class="text-sm dark:text-jacarta-200">{likes}</span>
        </div>
    </div>
</article>
